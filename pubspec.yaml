name: cp_delivery
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  felix_ui:
    git:
      url: https://github.com/the-flx/FlxUI.git
      ref: 1.0.0
  get: ^3.26.0
  shimmer: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.9.2
  lint: ^1.0.0

flutter:
  uses-material-design: true
